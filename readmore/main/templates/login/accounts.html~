<!DOCTYPE html>
{% load i18n %}
{% load account %}
{% load url from future %}



{% if request.user.is_authenticated %}
    <form class="login navbar-form navbar-right" method="POST" action="{% url 'account_logout' %}"> 
	{% csrf_token %}
	<button class="btn" type="submit">{% trans "Log Out" %}</button>
    </form>
    <form class="navbar-form navbar-left"> 
    <button class="btn">
    {% if request.user.first_name or request.user.last_name %}
        {{ request.user.first_name }} {{ request.user.last_name }}
    {% else %}
        {{ request.user.username }}
    {% endif %}
    {% if request.user.is_staff %} 
	(staff) 
    {% endif %}
    </button>
    </form>

{% else %}
    <form class="navbar-form navbar-left"> 
	<button class="btn" type="submit">Nieuwe Gebruiker?</button>
    </form>

    <!--<a href="/accounts/login/" class="pull-right">Login</a>-->
	 <form class="login navbar-form navbar-right" method="POST" action="{% url 'account_login' %}"> 
		{% csrf_token %}
		<div class="form-group">
			<input id="id_login" class="login-input" maxlength="30" name="login" placeholder="Gebruikersnaam" type="text" />{{ form.login.errors }}
		</div>
		<div class="form-group">
			<input id="id_password"  class="login-input" name="password" placeholder="Wachtwoord" type="password" />{{ form.password.errors }}
		</div>
		{% if redirect_field_value %}
			<input type="hidden" name="{{ redirect_field_name }}" value="{{ redirect_field_value }}" />
		{% endif %}
		<button class="btn" type="submit">{% trans "Log In" %}</button>
	</form>
{% endif %}
</p>



